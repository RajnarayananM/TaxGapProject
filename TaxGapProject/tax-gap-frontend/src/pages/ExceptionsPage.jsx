import React,{useEffect,useState} from 'react'; import client from '../api/client'; export default function ExceptionsPage(){ const [data,setData]=useState([]); const [customerId,setCustomerId]=useState(''); const [severity,setSeverity]=useState(''); const [ruleName,setRuleName]=useState(''); const load=async()=>{ const params={}; if(customerId) params.customerId=customerId; if(severity) params.severity=severity; if(ruleName) params.ruleName=ruleName; const r=await client.get('/exceptions',{ params }); setData(r.data); }; useEffect(()=>{ load(); },[]); return (<div><h3>Exceptions</h3><div style={{display:'flex',gap:8}}><input placeholder='customerId' value={customerId} onChange={e=>setCustomerId(e.target.value)} /><select value={severity} onChange={e=>setSeverity(e.target.value)}><option value=''>Severity</option><option>HIGH</option><option>MEDIUM</option><option>LOW</option></select><input placeholder='ruleName' value={ruleName} onChange={e=>setRuleName(e.target.value)} /><button onClick={load}>Filter</button></div><table border='1' cellPadding='6' style={{marginTop:12}}><thead><tr><th>Transaction</th><th>Customer</th><th>Rule</th><th>Severity</th><th>Message</th><th>Created</th></tr></thead><tbody>{data.map(e=>(<tr key={e.id}><td>{e.transactionId}</td><td>{e.customerId}</td><td>{e.ruleName}</td><td>{e.severity}</td><td>{e.message}</td><td>{e.createdAt}</td></tr>))}</tbody></table></div>); }
