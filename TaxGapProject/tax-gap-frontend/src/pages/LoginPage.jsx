import React,{useState} from 'react'; import axios from 'axios'; export default function LoginPage(){ const [username,setUsername]=useState('auditor'); const [password,setPassword]=useState('auditor123'); const [err,setErr]=useState(null); const submit=async e=>{ e.preventDefault(); setErr(null); try{ const res=await axios.post('/auth/login',{username,password}); localStorage.setItem('jwt', res.data.token); localStorage.setItem('user', JSON.stringify({ username: res.data.username, roles: res.data.roles })); window.location.href='/'; }catch(e){ setErr('Login failed'); } }; return (<form onSubmit={submit} style={{maxWidth:320}}><h3>Login</h3><label>Username</label><input value={username} onChange={e=>setUsername(e.target.value)} /><label>Password</label><input type='password' value={password} onChange={e=>setPassword(e.target.value)} /><button type='submit'>Login</button>{err && <div style={{color:'red'}}>{err}</div>}</form>); }
